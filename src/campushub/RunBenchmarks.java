package campushub;

import campushub.bench.BenchmarkRunner;

/** Entry point that runs the empirical performance benchmarks. */
public class RunBenchmarks {
    public static void main(String[] args) {
        BenchmarkRunner.run();
    }
}
